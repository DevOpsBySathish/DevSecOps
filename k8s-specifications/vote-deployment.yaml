apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: vote
  name: vote
  namespace: demoaks-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vote
  template:
    metadata:
      labels:
        app: vote
    spec:
      containers:
      - image: acrvoteappdemo.azurecr.io/vote-app:latest
        name: vote
        ports:
        - containerPort: 80
          name: vote
